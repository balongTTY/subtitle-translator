# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 与 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
详细的逐条修复记录见 [`docs/fix-records/`](docs/fix-records/)。

## [未发布]

## [1.0.0] - 2026-08

首个公开版本。

### 新增

- **三阶段翻译流水线**：全篇分析 → 分批翻译 → 逐批质检，而非简单逐句直译
- **术语体系**：用户强制语料库 + 112 词预设参考表 + 术语深度优化 + 一键从分析结果导入
- **翻译风格控制**：日→中翻译腔对照（10+ 正反例、6 类规则）、强制无标点输出、源语言自动检测（日/英）
- **批量并行**：多文件并行翻译，多个 worker 共享速率限制器，不会超发 API 配额
- **增量预览**：翻译过程实时显示；多文件并行时可下拉切换查看；单条重译并写回文件
- **字幕提取**：基于 faster-whisper 的本地语音识别（视频/音频 → SRT）
- **在线 ASR**：OpenAI 兼容 `transcriptions` 与 MiMo `chat_audio` 双协议引擎
- **模型下载**：设置页一键预下载识别模型，带真实进度
- **格式保真**：ASS 标签/样式/绘图块保护，SRT 内联标签与粗体保留
- **多提供商支持**：OpenAI / Anthropic Claude / DeepSeek / 任意 OpenAI 兼容端点
- **打包分发**：PyInstaller 单文件 exe（内置 spec）

### 修复

- **QThread 生命周期**：取消/超时不再触发 Qt 硬崩溃，模块级线程注册表兜底引用
- **LLM 编号解析**：兼容 LLM 从 1 起始重编号与多行续写，堵住错位译文击穿质检的漏洞
- **Claude Prompt Caching**：拆分「跨批恒定前缀」与「批级可变内容」，缓存真正命中
- **密钥存储**：Windows 使用 DPAPI；非 Windows 改为持久化随机密钥文件
- **语料库路径**：打包环境下写入用户数据目录，用户编辑不再随重启丢失
- **限流器**：超容量请求有界等待，杜绝 worker 永久挂死
- **质检**：堵住空译文/未翻译/占位符缺失三类静默数据问题
- 完整明细见 [`docs/fix-records/2026-08.md`](docs/fix-records/2026-08.md)

### 已知限制

- pysubs2 在加载 SRT 时会剥离 `<font>` 等样式标签（上游库行为，无法在翻译层还原）
- 输出 SRT 时 `{\b}` 粗体标记会被 pysubs2 丢弃，工具已自动转回 `<b>` 保留
